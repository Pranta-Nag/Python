"""
    using the OS module
    module (like a code library) is a file written by another that generally has a functions we can use.

    import os
    os.remove(file name)

"""

###### if we install any module ; pip install 'module name' or pip3 install 'module name' => like - pip install tensorflow

import os

os.remove("sample2.txt")

